import Diagnosis from '../views/Diagnosis.vue';

export const routes = { // тут тоже
    HOME: {
        path: '/',
        name: 'home',
        component: Diagnosis,
    },
};

export const koctyl = 1;
